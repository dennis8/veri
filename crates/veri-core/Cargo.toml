[package]
name = "veri-core"
version = "0.0.1"
edition = "2021"

[lib]
name = "veri_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
anyhow = "1.0"
log = "0.4"

[dev-dependencies]
tempfile = "3.8"
